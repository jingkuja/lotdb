# 本轮优化说明

## 功能与可靠性

- 查询标签保留独立数据库会话；缓存会话进程 ID，修复 PostgreSQL 事务出错后无法执行 ROLLBACK 的问题。
- 支持数据库/schema 上下文选择；切换上下文会确认并重建会话，未提交事务回滚。
- 区分执行当前语句/选区与执行全部；修改类 SQL 统一应用写操作确认设置；显示显式事务状态并提供提交/回滚入口。
- 空查询结果保留列头，PostgreSQL RETURNING 同时返回数据与影响行数。
- SQL 草稿与工作区恢复；关闭标签和窗口前检查未提交编辑、执行任务及显式事务。
- 表格提交在同一事务内锁定并核对原始值，避免覆盖其他客户端已修改的数据。
- SSH 校验 known_hosts 或用户提供的 SHA256 主机指纹，拒绝未知或不匹配主机。

## 界面

- 首页提供新建连接和最近访问入口；标签显示连接、数据库和未保存状态。
- 编辑器跟随应用主题；查询结果复用虚拟表格、列宽调整和完整单元格查看器，支持复制和 CSV 导出。
- 连接失败、SSH 校验失败、传输失败和取消均提供明确反馈。

## 性能与边界

- 表格默认按需统计总数；单主键且按主键排序时使用游标翻页，其余情况采用带稳定排序的 OFFSET 分页。
- 查询结果增加 16 MiB 序列化数据上限，界面提示截断；这不是单字段按需从数据库加载。
- 跨表传输采用流式读取，每批最多 500 行或约 512 KiB SQL；仅对死锁和序列化失败重试。
- 传输以批次为事务边界：取消在读取/批次边界生效，已提交批次保留；失败不会自动回滚先前批次。
- 隐藏标签暂停进程轮询；管理页面、编辑器和 SQL 格式化器按需加载。
- 生产构建主入口约 410.41 kB，查询页约 472.60 kB，格式化器约 261.34 kB（未压缩）。拆包体积不等同于实测启动耗时提升。

## 验证

- `pnpm test`：71 项通过。
- `cargo test --offline --lib`：33 项通过。
- 隔离 MySQL 8.4、PostgreSQL 16 集成测试：4 项通过，覆盖事务恢复、空列头、RETURNING、游标分页、编辑冲突和 1201 行分批传输。
- `pnpm build`、`cargo clippy --offline --all-targets -- -D warnings`、`git diff --check` 通过。
- `pnpm lint`：0 错误，保留 3 项已有 React Fast Refresh 导出警告。
- 独立桌面测试包已启动并确认首页入口；完整界面交互与大规模数据压力测试尚未覆盖。
